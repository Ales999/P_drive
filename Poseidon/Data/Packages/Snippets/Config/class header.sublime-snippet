<snippet>
	<content><![CDATA[class Header
{
		/* minPlayers < maxPlayers */
		gameType = CTF; //@Const: Unknown || Coop || DM || Team || CTF || SCont || CTI || FF || Hold
		minPlayers = 1; //@Int # of players the mission supports
		maxPlayers = 10; //@Int # of players the mission supports
};
]]></content>
	<tabTrigger>header</tabTrigger> 
	<description>desc/class header</description>
	<scope>source.ext</scope>
</snippet>